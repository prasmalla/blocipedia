<% if current_user.standard? %>
  <%= link_to "Upgrade Account", new_charge_path %>
<% else %>
  <%= link_to "Downgrade Account", downgrade_path, method: :post, data: { confirm: "Your private wikis will become public, are you sure?" } %>
<% end unless current_user.admin? %>
